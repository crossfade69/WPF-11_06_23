<UserControl x:Class="WpfProjekt.MVVM.View.ProfileView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfProjekt.MVVM.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      >

    <Grid>
        <!-- Profile Picture 
        <Image Source="profile_picture.jpg" Width="100" Height="100" HorizontalAlignment="Left" Margin="20" />-->

        <!-- Profile Information -->
        <StackPanel HorizontalAlignment="Left" Margin="140,20,0,0">
            <!-- Username -->
            <TextBlock Text="Username" FontSize="20" FontWeight="Bold" Foreground="White" />
            <TextBlock Text="Level: 50" Foreground="White" />
            <Button x:Name="ProfEditButton" Content="Edit" Width="76" Margin="510 0 0 0" Height="33" Click="ProfEditButton_Click"/>
            <TextBlock Text="Description: Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed vel turpis risus. Integer tempus augue nisi, et &#xA; lacinia leo commodo nec." Foreground="White" Height="50" ScrollViewer.VerticalScrollBarVisibility="Auto" Width="640" ScrollViewer.HorizontalScrollBarVisibility="Auto" RenderTransformOrigin="0,0.5" />

            <!-- Level -->

            <!-- Description -->

            <!-- Games Played -->
        </StackPanel>


        <ListView Name="GamesInStoreListView"  Background="#FF232323" Foreground="White" Margin="36,156,36,30">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding name}" Grid.Column="0"/>
                        <TextBlock Text="{Binding category}" Grid.Column="1"/>
                        <TextBlock Text="{Binding rating}" Grid.Column="2"/>
                        <Image Source="{Binding image}" Width="100" Height="100" Grid.Column="3"/>

                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
